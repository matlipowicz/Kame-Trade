import { Flex } from "@chakra-ui/react";
const Profile = () => {
    return (
        <Flex alignItems={"center"} justifyContent={"center"}>
            Profile
        </Flex>
    );
};
export default Profile;
